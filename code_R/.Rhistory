# Chargement des packages nécessaires
library(readxl)
library(dplyr)
library(pheatmap)
# Étape 1: Chargement des données
data <- read_excel("../article.source/pgen.1006453.s002.xlsx")
View(data)
# Inspecter les premières lignes des données pour comprendre la structure
head(data)
# Chargement des packages nécessaires
library(readxl)
library(dplyr)
library(pheatmap)
# Étape 1: Chargement des données
# Spécifiez le chemin vers votre fichier Excel
file_path <- "../article.source/pgen.1006453.s002.xlsx"
# Lire toutes les feuilles du fichier Excel pour inspection
excel_data <- read_excel(file_path, sheet = NULL)
# Étape 1: Chargement des données
# Spécifiez le chemin vers votre fichier Excel
file_path <- "../article.source/pgen.1006453.s002.xlsx"
# Lire toutes les feuilles du fichier Excel pour inspection
excel_data <- read_excel(file_path, sheet = NULL)
View(excel_data)
# Inspecter la structure des données pour identifier la feuille correcte et les colonnes nécessaires
str(excel_data)
# Supposons que les données pertinentes sont dans la première feuille et que les commentaires sont dans les premières lignes
sheet_name <- names(excel_data)[1]
data <- excel_data[[sheet_name]]
# Inspecter les premières lignes des données pour identifier les lignes de commentaire
head(data)
View(excel_data)
# Chargement des packages nécessaires
library(readxl)
library(dplyr)
library(pheatmap)
# Étape 1: Chargement des données
# Spécifiez le chemin vers votre fichier Excel
file_path <- "../article.source/pgen.1006453.s002.xlsx"
# Lire toutes les feuilles du fichier Excel pour inspection
data <- read_excel(file_path, sheet = 1, col_names = TRUE, skip = 3)
View(data)
# Lire toutes les feuilles du fichier Excel pour inspection
data <- read_excel(file_path, sheet = 1, col_names = TRUE, skip = 2)
warnings(à)
warnings()
View(data)
head(data$mean_expression)
hist(data$mean_expression)
hist(log10(data$mean_expression))
hist(log10(data$mean_expression), nc=100)
# Inspecter les premières lignes des données pour vérifier la structure
head(data)
# Supposons que les colonnes pertinentes sont les colonnes numériques représentant les points temporels
numeric_columns <- sapply(data, is.numeric)
expression_data <- data[ , numeric_columns]
# Inspecter les données nettoyées
head(expression_data)
head(data)
names(data)
# Étape 2: Normalisation (si nécessaire)
# Appliquer une normalisation z-score à chaque ligne (gène)
normalize_data <- function(data) {
apply(data, 1, scale)
}
normalized_data <- as.data.frame(normalize_data(expression_data))
# Étape 3: Visualisation
# Créer la heatmap
pheatmap(
mat = normalized_data,
cluster_rows = TRUE,
cluster_cols = FALSE,
scale = "none",
show_rownames = TRUE,
show_colnames = TRUE,
main = "Heatmap d'expression de gènes au cours du temps"
)
View(data)
summary(data$Figure2A_order_peaktime)
typeof(data$Figure2A_order_peaktime)
table(data$Figure2A_order_peaktime)
is.na(data$Figure2A_order_peaktime)
table(is.na(data$Figure2A_order_peaktime))
